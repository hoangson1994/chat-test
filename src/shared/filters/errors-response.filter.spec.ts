import { ErrorsResponseFilter } from './errors-response.filter';

describe('ErrorsResponseFilter', () => {
  it('should be defined', () => {
    expect(new ErrorsResponseFilter()).toBeDefined();
  });
});
